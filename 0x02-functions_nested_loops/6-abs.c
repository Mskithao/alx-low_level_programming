#include "main.h"

/**
 * _abs - main function
 *
 * @t: computes the absolute value of an intege
 *
 * Return: the absolute value
 */

int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
